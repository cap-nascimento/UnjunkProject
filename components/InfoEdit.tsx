import { StyleSheet } from 'react-native';

import { Text, View } from './Themed';

export default function InfoEdit({ path }: { path: string }) {
	return (
		<View>
			<Text>
					Conteúdo da info
			</Text>
		</View>
	);
}

const styles = StyleSheet.create({

});
